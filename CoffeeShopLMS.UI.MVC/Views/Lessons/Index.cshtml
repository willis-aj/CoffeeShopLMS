@model IEnumerable<CoffeeShopLMS.DATA.EF.Lesson>

@{
    ViewBag.Title = "Lessons";
}

<h2>@ViewBag.Title</h2>

@if (User.IsInRole("Admin"))
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <a class="btn btn-default" href=@Url.Action("AdminIndex", "Lessons")>View All Lessons</a> <br /><br />
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LessonTitle)
        </th>
        @*<th>
             @Html.DisplayNameFor(model => model.Introduction)
         </th>
         <th>
             @Html.DisplayNameFor(model => model.VideoURL)
         </th>
         <th>
             @Html.DisplayNameFor(model => model.PdfFileName)
         </th>
        <th>
                  @Html.DisplayNameFor(model => model.IsActive)
              </th>
             <th>
                  @Html.DisplayNameFor(model => model.QuizRequired)
              </th>
              <th>
                  @Html.DisplayNameFor(model => model.VideoRequired)
              </th>*@
        <th>
            Course
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LessonTitle)
        </td>
        @*  <td>
            @Html.DisplayFor(modelItem => item.Introduction)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VideoURL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PdfFileName)
        </td>
         <td>
                 @Html.DisplayFor(modelItem => item.IsActive)
             </td>
            <td>
                     @Html.DisplayFor(modelItem => item.QuizRequired)
                 </td>
                 <td>
                     @Html.DisplayFor(modelItem => item.VideoRequired)
                 </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Cours.CourseName)
        </td>
        <td>

            @if (!User.IsInRole("Admin"))
            {
                @Html.ActionLink("Details", "Details", new { id = item.LessonID }, new { @class = "btn btn-default" })
            }
            @if (User.IsInRole("Admin"))
            {
            <span>
                @Html.ActionLink("Details", "Details", new { id = item.LessonID }) |
                @Html.ActionLink("Edit", "Edit", new { id = item.LessonID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.LessonID })
            </span>
            }
        </td>
    </tr>
    }

</table>
